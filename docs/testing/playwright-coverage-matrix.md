# Playwright Coverage Matrix
| Feature/Capability | Route(s) | UI Component(s) | API Endpoint(s) | Persona(s) | Test IDs | Notes / Risks / UNKNOWNs |
| --- | --- | --- | --- | --- | --- | --- |
| Task CRUD + completion | `/` (task dashboard, `apps/web/app/page.tsx`) | `TasksTable` (`apps/web/app/components/TasksTable.tsx`) | `POST /todos`, `PATCH /todos/:id`, `DELETE /todos/:id` (`apps/api/src/todos/todos.controller.ts`) | Standard user | T-SMOKE-01 | Server-side ownership and stage guards live in `TodosService` (`apps/api/src/todos/todos.service.ts`); audit entries from `AuditService` keep track of every change. |
| Scheduling + rescheduling | `/calendar` + `ScheduleModal` (`apps/web/app/calendar/page.tsx`, `apps/web/app/components/ScheduleModal.tsx`) | Calendar grid + Schedule modal overlays | `PATCH /todos/:id/schedule` (schedule/unschedule, `apps/api/src/todos/todos.controller.ts`) | Standard user | T-REG-01, T-CAL-02 | Overlap guard (`ConflictException` in `TodosService.schedule`), `min/max` duration from `common/constants.ts`. Drag interactions handled by `DragProvider`. |
| Attachment/OCR workflow | `/task/[id]` attachment panel (`apps/web/app/task/[id]/page.tsx`) | Attachment rows + OCR viewer controls | `POST /attachments/todo/:todoId`, `POST /attachments/:id/ocr`, `GET /attachments/:id/ocr`, `POST /attachments/:id/ocr/apply` (`apps/api/src/attachments/attachments.controller.ts`) | Standard user | T-OCR-01 | OCR worker dependency via `OcrService`; `ApplyOcrDto` restricts remark vs description, `CreateRemarkDto` limits 150 chars. |
| Relationship constraints | `/` tasks table relationship column (`apps/web/app/components/TasksTable.tsx`) | Relationship modal | `POST /todos/:id/associate`, `POST /todos/:id/disassociate` (`apps/api/src/todos/todos.controller.ts`) | Standard user | T-REL-01 | v4 rules enforced in `TodosService.associateTask`/`disassociateTask` (max depth 2, detach before delete). |
| Audit visibility | `/activity` log viewer (`apps/web/app/activity/page.tsx`) | Audit list entries / `ActionBadge` | `GET /audit`, `GET /audit/all` (`apps/api/src/audit/audit.controller.ts`) | Admin user | T-AUDIT-01 | Admin-only due to `AdminGuard`; entries include `details` from `AuditService`. |
| Admin user management | `/admin`, `/customizations` (`apps/web/app/admin/page.tsx`, `apps/web/app/customizations/page.tsx`) | Admin table + settings panels | `GET /admin/users`, `POST /admin/users/:id/reset-password`, `POST /admin/users/:id/toggle-admin`, `PUT /settings`, `PUT /settings/duration` (`apps/api/src/admin/admin.controller.ts`, `apps/api/src/settings/settings.controller.ts`) | Admin user | T-PERM-01 | Guarded via `AdminGuard`. Settings PUTs expect DTOs (`update-settings.dto.ts`, `update-duration-settings.dto.ts`). |
| Workflow execution API | API-only | (none yet) | `POST /workflows/:id/execute`, `POST /workflows/executions/:executionId/steps/:stepId/action` (`apps/api/src/workflows/workflows.controller.ts`) | Authenticated owner | T-WF-01 | Workflow tables are inert (`workflow_definitions`, `workflow_executions`, `workflow_step_executions` in `apps/api/src/db/schema.ts`). |
